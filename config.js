// ============================================================
// EDIT THIS FILE with your own Supabase project details.
// Same values as extension/config.js — this dashboard talks to
// the SAME Supabase project and the SAME Edge Functions.
// Find both values in: Supabase Dashboard → Project Settings → API
// ============================================================

const CONFIG = {
  SUPABASE_URL: 'https://zisjrcmunjojnjjhwsxd.supabase.co',
  // The "anon" / "public" key — safe to use in a browser.
  // NEVER put the service_role key here.
  SUPABASE_ANON_KEY: 'sb_publishable_McqF82ohsBKObES7dK-SDA_DXbMpl-N',
};
